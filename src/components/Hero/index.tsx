import { HeroForm, HeroFormTitle, HeroContainer } from './styles'

const Hero = () => (
  <HeroForm>
    <HeroContainer>
      <HeroFormTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroFormTitle>
    </HeroContainer>
  </HeroForm>
)

export default Hero
